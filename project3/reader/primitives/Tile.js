function Tile(scene){
    CGFobject.call(this, scene);
    this.initBuffers();
};

Tile.protoype = Object.create(CGFobject.protoype);
Tile.prototype.constructor = Tile;

Tile.prototype.initBuffers = function(){
    
}
